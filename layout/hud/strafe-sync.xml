<root>
	<styles>
		<include src="file://{resources}/styles/main.scss" />
	</styles>

	<scripts>
		<include type="module" src="file://{scripts}/hud/strafe-sync.ts" />
	</scripts>

	<!-- Strafe Offsets (sync trainer): history of key↔mouse timing at each strafe keyswitch. -->
	<Panel class="strafeoffset">
		<Panel id="OffsetArea" class="strafeoffset__area">
			<UICanvas id="OffsetCanvas" class="strafeoffset__canvas" />
		</Panel>
		<Label id="OffsetText" class="strafeoffset__text" text="" />
	</Panel>
</root>
